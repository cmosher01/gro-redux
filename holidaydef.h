/*
This file contains a list of all
holidays handled by the system.
Each holiday is passed to the
macro "h". This file is to be
#included by a file that needs
to handle all holidays, and the
includer must #define the h macro
before including this file.
*/
h(Easter)
h(EasterOrthodox)
h(Presidents)
h(Labor)
h(Thanksgiving)
h(KingMartinLuther)
h(ColumbusChristopher)
h(Census)
